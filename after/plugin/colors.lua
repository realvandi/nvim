--This function does not work. Rose-pine fucks with my terminal's color scheme, but this is here anyways since I'm following along with Primeagen's tutorial for nvim. 
function ColorMyPencils(color)
	--color = color or "rose-pine"
	--vim.cmd.colorscheme(color)

	--vim.api.nvim_set_hl(0, "Normal", { bg = "none" })
	--vim.api.nvim_set_hl(0, "NormalFloat", { bg = "none" })
end

ColorMyPencils()
